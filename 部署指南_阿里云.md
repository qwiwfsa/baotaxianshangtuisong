# 恒信资本网站后台部署指南 - 阿里云服务器

## 📋 部署概述

本文档详细说明如何将恒信资本网站后台管理系统部署到阿里云ECS服务器。

---

## 🖥️ 服务器信息

| 项目 | 配置 |
|------|------|
| 服务器IP | 47.95.236.85 |
| 域名 | api.yaozijin.com（备案中，暂时使用IP访问） |
| 网站根目录 | /www/wwwroot/api.yaozijin.com |
| 数据库名称 | yaozijin |
| 数据库用户名 | yaozijin |
| 数据库密码 | fdsajkl |
| 数据库端口 | 3306 |

---

## 📦 部署文件清单

### 需要上传的目录和文件：

```
website-capital-final/
├── admin/                    # 后台管理系统（核心）
│   ├── api/                  # API接口目录
│   │   ├── config.php        # 数据库配置文件（已修改）
│   │   ├── init-db.php       # 数据库初始化脚本
│   │   ├── *.php             # 其他API文件
│   │   ├── case/             # 案例管理API
│   │   └── news/             # 新闻管理API
│   ├── assets/               # 静态资源
│   ├── builder/              # 页面构建器
│   ├── components/           # 组件目录
│   ├── data/                 # 数据文件目录
│   ├── *.html                # 后台页面文件
│   └── *.js                  # JavaScript文件
├── cms/                      # CMS内容管理目录
│   ├── content.json          # 内容数据文件
│   └── uploads/              # 上传文件目录
└── server.js                 # Node.js图片上传服务
```

---

## 🔧 部署步骤

### 步骤1：准备服务器环境

确保阿里云服务器已安装以下环境：

```bash
# 1. 检查PHP版本（需要PHP 7.4+）
php -v

# 2. 检查MySQL版本（需要MySQL 5.7+）
mysql -V

# 3. 检查Nginx/Apache
nginx -v  # 或 httpd -v

# 4. 检查Node.js（用于图片上传服务）
node -v
npm -v
```

### 步骤2：创建网站目录

```bash
# 登录服务器后执行
sudo mkdir -p /www/wwwroot/api.yaozijin.com
sudo chown -R www:www /www/wwwroot/api.yaozijin.com
```

### 步骤3：上传文件到服务器

#### 方式A：使用SCP命令上传

```bash
# 在本地执行，上传admin目录
scp -r admin/ root@47.95.236.85:/www/wwwroot/api.yaozijin.com/

# 上传cms目录
scp -r cms/ root@47.95.236.85:/www/wwwroot/api.yaozijin.com/

# 上传server.js
scp server.js root@47.95.236.85:/www/wwwroot/api.yaozijin.com/
```

#### 方式B：使用FTP/SFTP工具

使用FileZilla、WinSCP等工具连接服务器，将文件上传到 `/www/wwwroot/api.yaozijin.com/` 目录。

### 步骤4：设置文件权限

```bash
# 登录服务器后执行

# 进入网站目录
cd /www/wwwroot/api.yaozijin.com

# 设置目录权限
sudo chown -R www:www /www/wwwroot/api.yaozijin.com
sudo chmod -R 755 /www/wwwroot/api.yaozijin.com

# 设置上传目录可写权限
sudo chmod -R 777 /www/wwwroot/api.yaozijin.com/cms/uploads
sudo chmod -R 777 /www/wwwroot/api.yaozijin.com/admin/data

# 设置PHP文件权限
sudo chmod -R 644 /www/wwwroot/api.yaozijin.comadmin/api/*.php
sudo chmod -R 644 /www/wwwroot/api.yaozijin.comadmin/api/**/*.php
```

### 步骤5：配置Nginx

创建或修改Nginx配置文件：

```bash
sudo nano /www/server/panel/vhost/nginx/api.yaozijin.com.conf
```

添加以下配置：

```nginx
server {
    listen 80;
    server_name api.yaozijin.com 47.95.236.85;
    root /www/wwwroot/api.yaozijin.com;
    index index.html index.php;

    # 前端页面
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后台管理
    location /admin {
        try_files $uri $uri/ =404;
    }

    # PHP API接口
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # 上传文件访问
    location /cms/uploads {
        alias /www/wwwroot/api.yaozijin.com/cms/uploads;
    }

    # 禁止访问敏感文件
    location ~ /\. {
        deny all;
    }
    location ~ /(config\.php|init-db\.php) {
        deny all;
    }
}
```

重启Nginx：

```bash
sudo nginx -t
sudo systemctl restart nginx
# 或
sudo /etc/init.d/nginx restart
```

### 步骤6：初始化数据库

```bash
# 登录服务器后执行

# 进入网站目录
cd /www/wwwroot/api.yaozijin.com

# 运行数据库初始化脚本
php admin/api/init-db.php
```

如果命令行执行失败，可以通过浏览器访问：
```
http://47.95.236.85admin/api/init-db.php
```

### 步骤7：启动Node.js图片上传服务

```bash
# 登录服务器后执行

# 进入网站目录
cd /www/wwwroot/api.yaozijin.com

# 安装依赖（首次需要）
npm install express multer cors

# 使用PM2启动服务（推荐，后台运行）
npm install -g pm2
pm2 start server.js --name "cms-upload-server"
pm2 save
npm2 startup

# 或使用nohup启动
nohup node server.js > server.log 2>&1 &

# 查看服务状态
pm2 status
# 或
ps aux | grep node
```

---

## ✅ 部署验证

### 1. 访问后台管理页面

```
http://47.95.236.85/admin/
或
http://api.yaozijin.com/admin/
```

### 2. 测试API接口

```bash
# 测试数据库连接
curl http://47.95.236.85admin/api/test.php

# 测试页面列表接口
curl http://47.95.236.85admin/api/list-pages.php
```

### 3. 测试图片上传服务

```bash
# 测试Node.js服务健康状态
curl http://47.95.236.85:3000/api/health
```

### 4. 检查数据库表

```bash
# 登录MySQL
mysql -u yaozijin -p

# 输入密码: fdsajkl

# 查看数据库
USE yaozijin;
SHOW TABLES;

# 应该看到以下表：
# - cms_pages
# - cms_sections
# - cms_categories
# - cms_articles

# 查看默认数据
SELECT * FROM cms_pages;
SELECT * FROM cms_categories;
```

---

## 🔒 安全配置

### 1. 修改默认密码

部署完成后，请立即修改以下默认配置：

- 后台登录密码
- 数据库密码（建议修改）
- API密钥

### 2. 配置防火墙

```bash
# 开放必要端口
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-port=3000/tcp  # Node.js服务
sudo firewall-cmd --reload
```

### 3. 配置SSL证书（可选）

域名备案完成后，配置HTTPS：

```bash
# 使用Certbot申请免费SSL证书
sudo certbot --nginx -d api.yaozijin.com
```

---

## 📝 配置文件说明

### 数据库配置文件：admin/api/config.php

```php
<?php
// 数据库配置 - 阿里云服务器配置
define('DB_HOST', '47.95.236.85');
define('DB_PORT', '3306');
define('DB_USER', 'yaozijin');
define('DB_PASS', 'fdsajkl');
define('DB_NAME', 'yaozijin');
```

---

## 🔄 日常维护

### 重启服务

```bash
# 重启Nginx
sudo systemctl restart nginx

# 重启PHP-FPM
sudo systemctl restart php-fpm

# 重启Node.js服务
pm2 restart cms-upload-server
```

### 查看日志

```bash
# Nginx错误日志
sudo tail -f /var/log/nginx/error.log

# PHP错误日志
sudo tail -f /var/log/php-fpm/error.log

# Node.js服务日志
pm2 logs cms-upload-server
# 或
tail -f /www/wwwroot/api.yaozijin.com/server.log
```

### 备份数据

```bash
# 备份数据库
mysqldump -u yaozijin -p yaozijin > backup_$(date +%Y%m%d).sql

# 备份上传文件
tar -czvf uploads_backup_$(date +%Y%m%d).tar.gz /www/wwwroot/api.yaozijin.com/cms/uploads
```

---

## ❗ 常见问题

### 1. 数据库连接失败

**问题**：无法连接到MySQL数据库

**解决**：
```bash
# 检查MySQL是否运行
sudo systemctl status mysql

# 检查用户权限
mysql -u root -p
GRANT ALL PRIVILEGES ON yaozijin.* TO 'yaozijin'@'localhost' IDENTIFIED BY 'fdsajkl';
FLUSH PRIVILEGES;
```

### 2. 文件上传失败

**问题**：无法上传图片

**解决**：
```bash
# 检查目录权限
sudo chmod -R 777 /www/wwwroot/api.yaozijin.com/cms/uploads

# 检查PHP上传限制
# 修改 php.ini
upload_max_filesize = 10M
post_max_size = 10M
```

### 3. Node.js服务无法启动

**问题**：端口被占用

**解决**：
```bash
# 查看端口占用
sudo netstat -tlnp | grep 3000

# 结束占用进程
sudo kill -9 <PID>

# 或修改server.js使用其他端口
```

### 4. 403 Forbidden错误

**问题**：访问被拒绝

**解决**：
```bash
# 检查文件权限
sudo chown -R www:www /www/wwwroot/api.yaozijin.com
sudo chmod -R 755 /www/wwwroot/api.yaozijin.com
```

---

## 📞 技术支持

如遇到部署问题，请检查：

1. 服务器环境是否满足要求
2. 配置文件是否正确修改
3. 文件权限是否正确设置
4. 服务是否正常运行

---

## 📅 更新记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-04-24 | v1.0 | 初始部署指南 |

---

**部署完成！** 🎉
